using UnityEngine;

namespace CreateThis.VR.UI {
    public class ColorPickerProfile : MonoBehaviour {
        public GameObject thumbBody;
        public Material thumbMaterial;
        public Vector3 thumbLocalPosition = new Vector3(-0.108f, 0.009f, -1.74f);
        public Vector3 thumbScale = new Vector3(0.001f, 0.001f, 0);

        // Use this for initialization
        void Start() {

        }

        // Update is called once per frame
        void Update() {

        }
    }
}