using UnityEngine;

namespace CreateThis.VR.UI {
    public class FilePanelProfile : MonoBehaviour {
        public GameObject folderPrefab;
        public float kineticScrollerSpacing = 0.1f;
        public float scrollerHeight = 0.2f;
        public string searchPattern = "*.txt";

        // Use this for initialization
        void Start() {

        }

        // Update is called once per frame
        void Update() {

        }
    }
}